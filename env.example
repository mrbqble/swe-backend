# ==============================================================================
# Environment Configuration Template
# ==============================================================================
# This file serves as a template for environment variables used by the application.
#
# Usage:
#   1. Copy this file to .env: cp env.example .env
#   2. Edit .env with your actual values
#   3. Never commit .env to version control (it's in .gitignore)
#
# Security Note:
#   - Change all default values, especially SECRET_KEY, in production
#   - Use strong, randomly generated secrets
#   - Never share .env files or commit them to version control
#
# ==============================================================================

# ==============================================================================
# Application Environment
# ==============================================================================
# Environment mode: dev, staging, production
# Affects logging format, error handling, and other environment-specific behavior
ENV=dev

# ==============================================================================
# Database Configuration
# ==============================================================================
# PostgreSQL connection string for async SQLAlchemy
# Format: postgresql+asyncpg://username:password@host:port/dbname
# Example: postgresql+asyncpg://postgres:mypassword@localhost:5432/mydb
#
# For Docker Compose, use: postgresql+asyncpg://postgres:postgres@db:5432/mydb
# For local development, use: postgresql+asyncpg://postgres:2148@localhost:5432/postgres
DATABASE_URL=postgresql+asyncpg://postgres:2148@localhost:5432/postgres


# ==============================================================================
# Security Configuration
# ==============================================================================
# Secret key for JWT token signing and encryption
# IMPORTANT: Change this to a strong, randomly generated secret in production!
# Generate a secure key: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=change-me-in-production

# JWT signing algorithm
# Options: HS256 (symmetric), RS256 (asymmetric)
# HS256 is recommended for most use cases
ALGORITHM=HS256

# Access token expiration time in minutes
# Default: 30 minutes
# Adjust based on your security requirements
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Refresh token expiration time in minutes
# Default: 7 days (10080 minutes)
# Refresh tokens allow users to obtain new access tokens without re-authenticating
REFRESH_TOKEN_EXPIRE_MINUTES=10080

# ==============================================================================
# CORS (Cross-Origin Resource Sharing) Configuration
# ==============================================================================
# Comma-separated list of allowed origins for CORS
# The application will parse this string into a list automatically
# Examples:
#   - Single origin: http://localhost:3000
#   - Multiple origins: http://localhost:3000,http://localhost:8000,https://example.com
#   - All origins (development only): *
#
# Production: Specify exact origins (e.g., https://yourdomain.com)
# Development: Can include localhost origins
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# ==============================================================================
# Logging Configuration
# ==============================================================================
# Log level for application logging
# Options (in order of verbosity):
#   - DEBUG: Detailed information for debugging
#   - INFO: General informational messages (default)
#   - WARNING: Warning messages for potential issues
#   - ERROR: Error messages for exceptions
#   - CRITICAL: Critical errors that may cause the application to stop
#
# Production: Use INFO or WARNING
# Development: Use DEBUG for detailed logs
LOG_LEVEL=INFO

# ==============================================================================
# Optional: Application Metadata (rarely changed)
# ==============================================================================
# These settings have defaults in the code and typically don't need to be set
# unless you want to override them

# API version prefix (default: /api/v1)
# API_V1_PREFIX=/api/v1

# Project name (default: FastAPI Backend Server)
# PROJECT_NAME=FastAPI Backend Server

# Application version (default: 1.0.0)
# VERSION=1.0.0

# Application description (default: A minimal, production-ready FastAPI server)
# DESCRIPTION=A minimal, production-ready FastAPI server

# ==============================================================================
# Observability Configuration
# ==============================================================================

# Slow query threshold in milliseconds (default: 1000ms)
# Queries taking longer than this will be logged as warnings
# SLOW_QUERY_THRESHOLD_MS=1000

# Health check timeout in seconds (default: 5.0)
# Maximum time to wait for database health check
# HEALTH_CHECK_TIMEOUT_SECONDS=5.0

# ==============================================================================
# Security Hardening Configuration
# ==============================================================================

# Bcrypt cost factor (default: 12, recommended for 2024+)
# Higher rounds = more secure but slower (exponential cost)
# Range: 4-31 (12 is recommended balance)
# BCRYPT_ROUNDS=12

# Maximum request body size in bytes (default: 10MB)
# Prevents DoS attacks via large request bodies
# MAX_REQUEST_SIZE=10485760

# Maximum string length for input fields (default: 10000)
# Prevents excessive memory usage from large strings
# MAX_STRING_LENGTH=10000

# Enable OpenAPI docs in production (default: False)
# Set to True only if docs are protected with authentication
# ENABLE_DOCS_IN_PROD=False
